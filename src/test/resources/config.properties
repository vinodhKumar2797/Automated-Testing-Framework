# Base URL of the application under test
base.url=https://example.com/login

# Default browser type: chrome or firefox
browser=chrome

# Implicit wait timeout in seconds
implicit.wait=10

# Explicit wait timeout in seconds
explicit.wait=15

# Credentials for login tests (replace with valid credentials for real tests)
valid.username=testuser
valid.password=Test@1234

invalid.username=invaliduser
invalid.password=Invalid@1234
